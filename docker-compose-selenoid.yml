# ⚠️ Этот файл объединён с docker-compose.yml
#
# Все сервисы (Jenkins + Selenoid + UI + video-recorder)
# теперь находятся в одном файле docker-compose.yml.
#
# Запуск:
#   docker compose up -d
#
# Или только Selenoid (если Jenkins не нужен):
#   docker compose up -d selenoid selenoid-ui video-recorder
